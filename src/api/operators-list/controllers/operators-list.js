'use strict';

/**
 * operators-list controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::operators-list.operators-list');
